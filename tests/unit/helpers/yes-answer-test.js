
import { yesAnswer } from 'message-board/helpers/yes-answer';
import { module, test } from 'qunit';

module('Unit | Helper | yes answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = yesAnswer([42]);
  assert.ok(result);
});

